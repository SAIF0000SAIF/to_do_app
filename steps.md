python -m venv venv    --to create a virtual invironment 
venv\Scripts\activate  --to activate venv
pip install django     --to install django
django-admin startproject "project name" . --to create a project
python manage.py startapp myapp  --to creatre a app 
python manage.py migrate --to make initial migrstion 
python manage.py runserver  --to run project